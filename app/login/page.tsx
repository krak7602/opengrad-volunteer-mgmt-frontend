
export default function Page({
    params,
    searchParams,
}: {
    params: { slug: string }
    searchParams: { [key: string]: string | string[] | undefined }
}) {
    return (
            // <h1>Volunteer login page</h1>
            <div>
                <h1>Checking if it comes here</h1>
            </div>
        )
}
